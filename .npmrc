# npm configuration for consistent behavior across machines

# Use exact versions for reproducible installs
save-exact = false

# Generate package-lock.json for reproducible installs
package-lock = true

# Use npm scripts to run commands
scripts-prepend-node-path = true

# Fail on peerDep issues
legacy-peer-deps = false
